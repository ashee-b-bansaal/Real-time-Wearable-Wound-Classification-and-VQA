# Laptop Implementation

This folder is the desktop/laptop entrypoint layer.

The underlying Python implementation remains in:

- `src/wound_rt/`
- `scripts/`

Use the wrappers in `laptop/scripts/` if you want a clean separation between laptop and iPhone workflows.

